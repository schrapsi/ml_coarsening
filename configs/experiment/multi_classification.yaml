# @package _global_
defaults:
  - override /data: multi_class
  - override /model: multi_class
  - override /callbacks: default
  - override /trainer: default

data:
  graphs_file: ${paths.root_dir}/configs/data/graphs/mss_46_69.txt
  features_file: ${paths.root_dir}/configs/data/features/all.txt

model:
  class_weights: [1.0, 44.0, 76.8, 103.2, 126.8, 160.0, 199.2, 216.8, 213.6, 178.4]
  focal_gamma: 2.0

callbacks:
  early_stopping:
    monitor: "val/f1"
    mode: "max"
  model_checkpoint:
    monitor: "val/f1"
    mode: "max"